[build-system]
requires = ["uv_build>=0.11.7,<0.12"]
build-backend = "uv_build"

[project]
name = "keenyspace-shared"
version = "0.1.0"
requires-python = ">=3.14,<3.15"
dependencies = [
    "pydantic>=2.13",
]

[tool.uv.build-backend]
module-name = "keenyspace_shared"
module-root = ""
