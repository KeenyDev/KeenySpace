# GSD planning artifacts (local only)
.planning/

# Obsidian per-user state
.obsidian/workspace*.json
.obsidian/cache

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
*.egg-info/
dist/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Eval judge verdict cache (non-deterministic LLM output, not committed)
.eval-cache/

# IDE
.idea/

# Local agent instructions and state (per-user, not part of the repo)
CLAUDE.md
.claude/agent-memory/

# Obsidian per-user vault state
.obsidian/

# Local code-search index config
.vex.toml

# Pre-repo working material; the as-built design lives in docs/design/
concepts/
plans/
vision.md
open-questions.md


# Local secrets / env overrides (compose interpolation) — never commit
.env
deploy/.env
deploy/keenyspace.env
*.local.env
.env.bak*

# macOS
.DS_Store

# Per-user local agent state
.claude/settings.local.json
.claude/worktrees/
